pipeline {
    agent any
    stages {
        stage('[Backend] Prepare build agent') {
            agent {
                docker {
                    image 'openjdk:17.0.1-jdk-slim'
                    args '-v /home/twitter-clone/.m2:/root/.m2 --memory=192m -u root:root'
                    reuseNode true
                }
            }
            stages {
                stage('[Backend] Build') {
                    steps {
                        sh './mvnw clean'
                    }
                }
            }
        }
    }
}
