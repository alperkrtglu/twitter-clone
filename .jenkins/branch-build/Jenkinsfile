pipeline {
    agent any
    tools {
        maven 'M3'
    }
    stages {
        stage('[Backend] Clean') {
            steps {
                sh 'mvn clean'
            }
        }
        stage('[Backend] Build') {
            steps {
                sh 'mvn compile'
            }
        }
    }
}
